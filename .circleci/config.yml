version: 2.1
orbs:
  ci-tools: kanopi/ci-tools@x.y.z
workflows:
  phpcs-drupal:
    jobs:
      - ci-tools/phpcs-drupal:
          extensions: 'php,module,inc,install,test,profile,theme,css,info,txt,md'
          location: modules/custom/
          standards: 'Drupal,DrupalPractice'